import { urlChecker } from "../js/urlChecker"

describe("Testing the submit functionality", () => {
    test("there should be a value passed", () => {
           expect(typeof urlChecker).toBeTruthy()
})});
